# Fixtures

Real-world adapter inputs only: actual `multiqc_data.json` and Workflow Run RO-Crate
output from real pipeline runs, never hand-written stand-ins. No secrets, no PHI;
synthetic subject data only. Adapter fixtures land alongside the adapters that consume
them. `qc_metrics.schema.json` is the small JSON Schema fixture the facet tests validate
against.
